<%- include('../partials/header') %>
<h1 style="margin-top:10%">Users</h1>

 <table class="table table-hover table-striped">
	<thead>
    <h2>This page is a demo built using Node.js/Express.js and EJS templates.  It demonstrates a list/view design pattern and pulls data from a JSON file.  The pictures and content were generated by third party APIs, and involve no actual identities.</h2>
   	<tr>
      	<th scope="col">First Name</th>
      	<th scope="col">Last Name</th>
      	<th scope="col">Email</th>
   	</tr>
	</thead>
 <tbody>

<% users.forEach(function(user){ %>
  <tr>
   	<td><a href="users/view/<%= user.id %>"><%= user.first_name %></a></td>
   	<td><%= user.last_name %></td>
   	<td><%= user.email %></td>
  </tr>
<% }); %>

</tbody>
</table>

<%- include('../partials/footer') %>

